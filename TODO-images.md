# TODO - Fix images not displaying

- [ ] Update `JS/javascript.js` to centralize image URL normalization and fallback behavior
- [ ] Replace fragile inline `onerror="..."` handlers with JS-based fallback logic
- [ ] Ensure every product/cart/image viewer path uses `normalizeImageSrc(product.image)`
- [ ] Smoke-test: load homepage, paginate/search, open cart, verify images

